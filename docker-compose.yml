version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: comicarr-db
    restart: unless-stopped
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-comicarr}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-comicarrpass}
      POSTGRES_DB: ${POSTGRES_DB:-comicarr_new}
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: comicarr-redis
    restart: unless-stopped
    ports:
      - "6379:6379"

  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: comicarr-web
    restart: unless-stopped
    ports:
      - "${COMICARR_PORT:-8090}:8090"
    environment:
      - SECRET_KEY=${SECRET_KEY:-placeholder_secret_key}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER:-comicarr}:${POSTGRES_PASSWORD:-comicarrpass}@postgres:5432/${POSTGRES_DB:-comicarr_new}
      - REDIS_URL=redis://redis:6379/0
      - COMICVINE_API_KEY=${COMICVINE_API_KEY}
    volumes:
      - .:/app
      - config_data:/config
    depends_on:
      - postgres
      - redis

  celery_worker:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: comicarr-worker
    command: celery -A app.worker.celery_app worker --loglevel=info -Q default,grabs --concurrency=4
    restart: unless-stopped
    environment:
      - SECRET_KEY=${SECRET_KEY:-placeholder_secret_key}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER:-comicarr}:${POSTGRES_PASSWORD:-comicarrpass}@postgres:5432/${POSTGRES_DB:-comicarr_new}
      - REDIS_URL=redis://redis:6379/0
      - COMICVINE_API_KEY=${COMICVINE_API_KEY}
      - DOWNLOADER_TYPE=${DOWNLOADER_TYPE:-none}
    volumes:
      - .:/app
      - config_data:/config
    depends_on:
      - postgres
      - redis

  celery_beat:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: comicarr-beat
    command: celery -A app.worker.celery_app beat --loglevel=info --scheduler celery.beat.PersistentScheduler
    restart: unless-stopped
    environment:
      - SECRET_KEY=${SECRET_KEY:-placeholder_secret_key}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER:-comicarr}:${POSTGRES_PASSWORD:-comicarrpass}@postgres:5432/${POSTGRES_DB:-comicarr_new}
      - REDIS_URL=redis://redis:6379/0
      - COMICVINE_API_KEY=${COMICVINE_API_KEY}
      - SEARCH_INTERVAL_MINUTES=${SEARCH_INTERVAL_MINUTES:-60}
      - GRAB_ON_MATCH=${GRAB_ON_MATCH:-true}
    volumes:
      - .:/app
      - config_data:/config
    depends_on:
      - postgres
      - redis

volumes:
  postgres_data:
  config_data:
